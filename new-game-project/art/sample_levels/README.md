# Mastaba_01 — sample diorama level (user-authored in Asset Forge 2.5.2)
#
# Source: ~/Desktop/Sample Levels/Mastaba_01.model (UPDATED 2026-08-25 17:41 —
# earmarked area enlarged to z 0..9.5 to hold the baseplate + structure).
# Transcribed block-for-block into art/blob_poc.gd (BUILD 36): 67 blocks,
# 9 model types, AF coords 1:1 with the established contract (top y=2 ->
# y=0; earmark center (-0.75, 4.75) -> game origin; hedge L-brackets at
# (±2.75, 0, ±4.75)).
#
# Materials: the .model's custom1 override (Nature/sand.png) on the kit
# blocks is replicated in-game with a triplanar sand shader
# (art/sand_override.gdshader); the marble domes get the marble texture.
# The built-in blocks (icosphere_half, stairs_half_corner, debris_stone)
# are the USER'S OWN exports from Asset Forge (~/Desktop/Sample Levels/Models,
# File -> Export with Merge unchecked) — exact geometry, flat MTL colors.
#
# Open questions (2026-08-25, user): (1) hedge-corner L-bracket orientations
# in-game vs Asset Forge — rotation values are in the .model but the GLB base
# orientation vs AF's convention needs an AF screenshot to confirm;
# (2) baseplate 7x3 (long along X) vs earmark 5.5x9.5 (long along Z) — the
# baseplate still overhangs the earmark's X sides by ~0.75 units each.
